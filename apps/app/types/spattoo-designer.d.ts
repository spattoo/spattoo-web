// @spattoo/designer is a JS component library (no shipped types yet). Declare it
// so the app type-checks; tighten to real types if/when the library emits .d.ts.
declare module "@spattoo/designer";
